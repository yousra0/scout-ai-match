
// The file previously contained mock data for all stakeholder types.
// Now, either clear the arrays, or remove non-player mock data completely (as requested).
export const mockPlayers = [];
export const mockCoaches = [];
export const mockClubs = [];
export const mockAgents = [];
export const mockSponsors = [];
export const mockEquipmentSuppliers = [];
